/*#include "panel.h"

Panel::Panel(QWidget *parent): QWidget(parent)
{
    score = 0;
    setFixedSize(50, 30);
    setPalette(Qt::white);
    setAutoFillBackground(true);

    label = new QLabel(this);
    label->setText(QString(score));
}

void Panel::add_score()
{
    score++;
    update_panel();
}

void Panel::update_panel()
{
    label->setText(QString(score));
}
*/
